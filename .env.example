# ---- Server ----
# One place for the port: dev run, Docker and the tunnel all read it.
APP_HOST=127.0.0.1
APP_PORT=8077

# ---- LLM ----
# "ollama" = a model on your own machine: no key, no quota, no rate limit, and
#            nothing leaves the network (the pitch for an SME HR assistant).
# "mistral" = hosted; needs a key AND an activated tier, or every call is 429.
LLM_PROVIDER=ollama

OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:7b-instruct
OLLAMA_TIMEOUT=300
# Must stay above the context budget in generator.build_context — Ollama's
# default of 2048 silently truncates the extracts.
OLLAMA_NUM_CTX=4096
OLLAMA_NUM_PREDICT=350
OLLAMA_KEEP_ALIVE=30m

MISTRAL_API_KEY=your_mistral_key_here
LLM_MODEL=mistral-small-latest

# ---- Embeddings (local, no data leaves the host) ----
EMBEDDING_MODEL=intfloat/multilingual-e5-small
EMBEDDING_DEVICE=cpu

# ---- Reranker (cross-encoder, multilingual, ~120M params) ----
RERANKER_MODEL=cross-encoder/mmarco-mMiniLMv2-L12-H384-v1
RERANK_CANDIDATES=20

# ---- Vector store ----
CHROMA_PATH=./chroma_db
COLLECTION_NAME=sme_hr_corpus

# ---- Chunking ----
CHUNK_SIZE=1400
CHUNK_OVERLAP=200

# ---- Retrieval / guardrails ----
TOP_K=5
MIN_SIMILARITY=0.72
MIN_RERANK_SCORE=0.0
MAX_QUESTION_LENGTH=500
RATE_LIMIT=10/minute

# ---- Paths ----
DOCUMENTS_PATH=./data/documents
