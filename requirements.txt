# --- API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
slowapi==0.1.9

# --- RAG core ---
chromadb==0.5.23
sentence-transformers==3.3.1
# torch: NOT listed here on purpose. Installing it from PyPI pulls the CUDA
# build (~906 MB) plus ~3 GB of nvidia-* wheels that are dead weight on a
# CPU-only host and would triple the Docker image. Install the CPU wheel
# FIRST, then this file:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# Or just run: bash scripts/install.sh

# --- LLM ---
mistralai==1.2.5

# --- Document parsing ---
pypdf==5.1.0
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Utils ---
python-dotenv==1.0.1
langdetect==1.0.9
tqdm==4.67.1

# --- Frontend ---
streamlit==1.41.1
requests==2.32.3
